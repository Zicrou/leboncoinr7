<%= form_with(model: modele) do |form| %>
  <% if modele.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(modele.errors.count, "error") %> prohibited this modele from being saved:</h2>

      <ul>
        <% modele.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :marque, style: "display: block" %>
    <%= form.collection_select(:marque_id, Marque.order(:name), :id, :name,
      {
        include_blank: true
      },
      onchange: '',
      class:""
    ) %>
  </div>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
